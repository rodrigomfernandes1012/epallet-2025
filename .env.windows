# ============================================
# Configuração para Desenvolvimento Windows
# Banco MySQL na Nuvem (Linux)
# ============================================

# Flask
FLASK_APP=run.py
FLASK_ENV=development
DEBUG=True
TESTING=False

# Chave secreta (gerar com: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=sua-chave-secreta-aqui-mude-para-producao

# ============================================
# Banco de Dados - MySQL na Nuvem
# ============================================

# IMPORTANTE: Substitua 'SEU_IP_OU_DOMINIO' pelo IP ou domínio do seu servidor MySQL
# Exemplos:
# - IP: mysql://epallet:Rodrigo@101275@192.168.1.100:3306/epallet_db
# - Domínio: mysql://epallet:Rodrigo@101275@mysql.seudominio.com:3306/epallet_db
# - Localhost (se estiver rodando localmente): mysql://epallet:Rodrigo@101275@localhost:3306/epallet_db

DATABASE_URL=mysql://epallet:Rodrigo@101275@SEU_IP_OU_DOMINIO:3306/epallet_db

# ============================================
# WhatsGw API - Configuração WhatsApp
# ============================================

# API Key do WhatsGw (obter em https://app.whatsgw.com.br)
WHATSGW_APIKEY=sua-api-key-aqui

# Número do telefone conectado no WhatsGw (formato: 5511987654321)
WHATSGW_PHONE_NUMBER=5511987654321

# ============================================
# NOTAS IMPORTANTES
# ============================================
#
# 1. SUBSTITUA 'SEU_IP_OU_DOMINIO' pelo endereço do seu servidor MySQL
#
# 2. Se o MySQL estiver em um servidor remoto, certifique-se de que:
#    - A porta 3306 está aberta no firewall
#    - O usuário 'epallet' tem permissão para conectar remotamente
#    - Execute no servidor MySQL:
#      mysql -u root -p
#      GRANT ALL PRIVILEGES ON epallet_db.* TO 'epallet'@'%' IDENTIFIED BY 'Rodrigo@101275';
#      FLUSH PRIVILEGES;
#
# 3. Para gerar uma SECRET_KEY segura, execute:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 4. Este arquivo contém informações sensíveis!
#    NÃO compartilhe e NÃO faça commit no Git
#
# ============================================
